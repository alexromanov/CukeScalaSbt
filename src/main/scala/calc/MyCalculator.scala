package calc

class MyCalculator {
  def add(first: Int, second: Int): Int = {
    first + second
  }

  def add(first: BigDecimal, second: BigDecimal): BigDecimal = {
    first + second
  }

  def sub(first: Int, second: Int): Int = {
    first - second
  }
}
